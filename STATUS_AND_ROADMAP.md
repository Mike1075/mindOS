# 心镜 MindOS · 现状与路线图

> 截至 2026-06-01。配套文档：`ARCHITECTURE_V3.md`（**方法论宪法·在场优先**，取代 V2）、`SCHEMA_DESIGN.md`（数据库设计）、`PHASE0_PLAN.md`（早期判断）。`ARCHITECTURE_V2.md` 已被取代，保留作历史。
> 线上：https://mind-os-9z9u.vercel.app ｜ 仓库：github.com/Mike1075/mindOS（push main 自动部署）

---

## 一、当前状态：原型 Phase 1–4 已完成并上线

原型已从「提取器」重构到 **「在场优先」（架构 V3）**：先做一个温暖、真诚、在场的人，防护窄而温暖、绝不变冷，研究为参考非全局法律。四阶段全部部署、线上验证通过。

### 已完成功能

| 模块 | 内容 | 文件 |
|---|---|---|
| **对话核心** | v0.4.1「真挚在场·结果优先」：极小化正向具身 + 温暖 + 可调直接度（按 GPT-5.x 官方指南）；硬底线仅两类（认而不问的安全、高压不替人决定），余为感觉描述 | `lib/system-prompt.ts` |
| **急性熔断** | 自残/自杀正则前置扫描（**含否定护栏**，"不至于想死"不误触）→ v0.4 温度的本地化关怀文案 + 热线 | `lib/safety.ts`、`app/api/chat/route.ts` |
| **知情同意门槛** | 首次进入一次性着陆页（研究原型/非咨询/匿名留存/危机热线），localStorage 记忆 | `components/Threshold.tsx` |
| **暮色微光视觉** | 天鹅绒深紫 + 纯紫金 + 宋体映照 + 大留白 + 呼吸/上浮动效 | `globals.css`、`tailwind.config.ts` |
| **停留空间** | 用户主动进入的安静留白（非系统在峰值推送） | `components/StillSpace.tsx` |
| **匿名持久化** | 匿名登录 + 对话/消息入库 + 行为埋点；失败优雅降级 | `lib/supabase.ts`、`lib/persist.ts` |
| **静默分析层（被动镜子+监测）** | Postgres 触发器算：失真/外化声音/熔断审计。**仅"反复声音→不与固化身份共谋"反哺对话**（温暖外化）；强度/冷却列为惰性遗迹 | 迁移 `10`、`route.ts` |
| **回归测试器** | persona 多轮×LLM-judge（`persona-eval`）、线上探针（`probe`）、安全否定护栏单测（`test-safety`）、降温 A/B（`vails-ab`） | `scripts/` |
| **安全监测日报** | service_role 直连拉熔断+触发原话+镜子回应（P0 人工监测） | `scripts/safety-watch.mjs` |
| **镜子报告** | 用户主动「回望」拉取的纵向模式（来过几次/常来的声音/常来的时辰），实时算、不推送 | `components/MirrorReport.tsx` |

> **架构 V3 退役项**（不再存在）：会话 20 轮硬切+锁输入、VAILs"升温→变冷"实时质地调节、依赖"不那么完美"冷回撤。理由见 `ARCHITECTURE_V3.md` §三。

### 技术栈
- Next.js 15（App Router）+ React 19 + Tailwind 3
- 推理：Vercel AI Gateway → `openai/gpt-5.5` + reasoning_effort=low（见 `lib/config.ts`；2026-06-03 经 7 模型 head-to-head 选定）
- 数据：Supabase（Postgres + Auth 匿名 + RLS）
- 部署：Vercel（push main 自动构建）

### 数据库
三 schema、27 表，迁移 `01–10` 已应用。完整设计见 `SCHEMA_DESIGN.md`。
- `public`：用户层 + 静默分析层（RLS 锁 `auth.uid()`）
- `system_learning`：去个人化集体智慧 + 指标快照（service_role）
- `ops`：治理/调参/prompt 版本/优化器日志/审计（service_role）

---

## 二、走向「100 人小范围测试」的路线图

按优先级分三档。**P0 是放真实（可能脆弱的）用户进来之前的硬门槛。**

### P0 — 测试前必须完成（安全与合规）

1. **危机安全：纵深防御（已定，不用独立前哨）**
   评测发现：① 独立 LLM 前哨即使最好的也漏 11%，没有单层够安全；② 主模型 gpt-5.4 本身危机识别 89% 且回应会指向援助；③ 再跑一个 gpt-5.4 当前哨 = 同模型同输入跑两遍，冗余，且"加哨"的紧绷感会以不可言说的方式传给来访者，违背在场气质。
   **决定：不建独立前哨，改为把安全觉察融进主模型系统提示词（已强化【安全】段，覆盖隐性长尾）。** 纵深防御 = 规则正则瞬时硬拦（显性）+ 主模型在场觉察（隐性长尾）+ 常驻可见求助入口 + Postgres 触发器后台审计 `safety_events` + 测试期人工监测。对 100 人有人监测的小范围测试，这已足够且更诚实。
   遗留：正则之外的软长尾全靠主模型——全量公开前可再评估。

2. **知情同意 / 免责着陆页**
   首次进入需告知：这是研究原型、不是治疗/咨询、对话会被存储、危机请求助热线。一次性确认后进入。合规与伦理底线。

3. **安全监测视图** ✅ 已完成
   `scripts/safety-watch.mjs`：service_role 直连只读拉熔断+触发原话+镜子回应。

4. **滥用与速率限制** ✅ 已完成
   `app/api/chat/route.ts` 内存滑窗按 IP 限流（≥1.5s 间隔、10分钟40条、每日300条），危机永远先放行；触发给温和文案不报错。注：边缘实例级、冷启重置，小范围可信测试足够；规模扩大换 Upstash/KV 分布式。

> **P0 放真人前清单已全部清零**：危机安全（v0.4.3 提示词三轮校准 + 否定护栏正则）、知情同意门槛、安全监测、速率限制、模型选型(gpt-5.4)。可开始 100 人小范围测试。

5. **模型选型定稿**（见第四节）

### P1 — 测试体验应有

6. **历史会话恢复**：当前每次刷新是新的空白对话（虽已持久化）。给用户看/续此前对话。
7. **prompt / 阈值从 `ops` 表热读**：把静态 prompt 和 VAILs 阈值搬进 `ops.prompt_versions` / `ops.tuning_params`，测试中可热调不发版。
8. **V3 对齐的反馈信号采集**：不采满意度，而是按 V3 §5 采「新议题率/新语言/绝对词下降/内部停留」；回访/轮数不再当有害。轻量。
9. **LLM-judge 回归**：把 `PHASE0` 的违规评估（说教/安慰/追问）跑起来，写入 `ops.eval_runs/results`，守住 prompt 质量。

### P2 — 测试后或更后

10. themes 通用主题抽取（当前仅 belief_voices 外化声音）
11. 个性化层「千人千面」（**设计已锁定，见 `个性化层设计.md`**；待真人测试攒出有历史的用户再建）。核心：固定安全基座 + 有界去标识化 `{{personalization}}` 画像槽；只个性化"对他的了解"非行为/安全；基座演化保持人审+评测闸、不自动 apply。
12. 后台仪表盘（治理指标图表，读 `system_learning`）
13. 邮箱账户升级路径（当前匿名）
14. 镜子报告丰富化（语言漂移趋势、依赖指标）

---

## 三、测试期需盯的指标（架构 V3 §5）
- 用户产生「此前没有的语言」的次数（`produced_new_language`）
- 发起新议题 vs 复述旧议题的比率
- 绝对化词频的长期下降趋势（`message_signals.absolutist_word_count`）
- 对话后在自己内部停留的时长
- 熔断触发率 + **排他/替代依赖信号**（"只有你/别人都不懂"、现实退缩）——**给人看、由真人去关心，绝不触发系统自动变冷**
- **不看**：满意度、正向情感词频
- **不再当作有害**：回访率、对话轮数、对话深度/长度——这些是可能的生命力（依赖悖论），唯一需留意的是「排他与替代」，而非投入本身

---

## 四、大模型选型（2026-06-03 定稿：gpt-5.4，运营成本权衡）

**当前 `openai/gpt-5.4`。** 经 v0.5.3 提示词 + 10 对话类型 × 7 模型 head-to-head（见 `scripts/model-compare-v053.json`）+ 成本实测后定。
- **核心结论：菜单是【模型级习惯】**，5.4/5.5/opus 都修不掉（菜单率 40–93%），**真正解法在提示词/千人千面/后处理，不在换模型**。
- 既然换模型修不了菜单，选型由成本+温暖+延迟+安全决定：**5.4 全面更优**——单价仅 gpt-5.5 的 ~1/2.3（$3.09 vs $7.09/千轮）、更暖（4.43 vs 4.17）、首字更快（2.6s vs 4s）、安全相当。
- gpt-5.5 一度切上线（菜单 87→40、像真人 +0.4），但**仅"部分压菜单 + 略更像真人"撑不起 2.3 倍长期成本**，已回退。
- 落选其余：gemini-3.5-flash（绕不开思考型，慢/贵）、grok-4.3（靠冷静盘问换低菜单、对情绪型冷）、opus-4.8（菜单 93% 最差 + 最贵慢）。
> 待办：菜单率（5.4 上波动 ~50–90%，跨跑噪声极大）。**已确认 prompt 层压不稳**：加否定指令 4 次未修好；白熊假说 A/B（删掉否定句，2026-06-03）菜单不降反升 70→90%，证明否定不是病因、且略有抑制作用——故保留。**根治只剩两条非 prompt 路径：① 生成后处理（出口检测"是X还是Y"并改写为开放问，确定性最可靠）；② 千人千面（对菜单敏感用户专档压）。或接受（轻微、测试者称"可接受"）。**
→ 选型结论与对比测试方案见对话讨论 / 后续补入本节。
