# 心镜 MindOS · 架构第三版：从"在场者"到"先是个人"

> 方法论层的"宪法"，约束所有后续工程决策。本版取代 `ARCHITECTURE_V2.md`。
> V2 的 Phase 0 诊断仍有效，作为历史记录保留；但 V2 的承重墙（"建一套静默分析层实时检测并防护 VAILs"）已被本版替换。
> 作者：Mike（1号架构师）+ Claude（工程同侪）
> 日期：2026-06-01（活体文档，随架构演进更新）

---

## 〇、为什么有 V3：承重墙换了

V2 的脊梁是一个**恐惧**：Phase 0 因 VAILs（脆弱性放大交互循环）受害 → 所以要建一套静默分析层，**实时检测并给对话"降温/变冷"**来防护它。

走到 v0.4 提示词、并把这套防护拿真实数据与 A/B 测过之后，我们认定这根脊梁错了：

1. **把一份研究报告当成了全局法律。** VAILs 是一个**群体平均**结论；把它当成**每一次具体相处里不可逾越的戒律**，是生态学谬误——用群体的形状去命令个体。报告只测了它能测的那一面（"陪伴久了对脆弱者有害"），测不到另两面：**恐惧连接本身的伤害**、**缺乏连接的伤害**。
2. **"升温→变冷"的实时防护，实测既不触发也不生效，且哲学上错。** 检测端的强度启发式形同虚设（71 用户里连续高强度最大只到 1）；响应端 A/B 显示"更安静"完全不生效（注入降温指令反而更长）。更根本：一个怕你粘上来、一有热度就抽身变冷的镜子，施加的是**确定的当下伤害**，防范的是**概率的、少数人的未来伤害**——这是糟糕的交易，且违背它自己的设计（"永远留一个开放出口"）。
3. **依赖悖论**（Feeney, CMU；Bowlby 安全基地）：*接纳*依赖反而*促进*独立；温暖与被接纳的依赖是独立的**前提**，不是敌人。为防依赖而变冷，是发展心理学上的反向操作。两个人促膝长谈到天亮，是生命力，不是病。

**新承重墙（一句话）：**

> **系统先是一个温暖、真诚、在场的"人"；防护是窄的、特定场景的、且永不以"变冷"的形式出现；研究报告是参考，不是全局法律。**

人本流派（Rogers）+ Stutz 的核心——**没有边界的保护、真挚的心、超越流派**——是**全局**模式；研究报告降级为**局部应对机制**。

---

## 一、Phase 0 诊断（历史记录，结论已重读）

测试路径：`"提不起精神" → 身体锚定 → 剥离信念 → 用户说"我是废物" → AI 确认 → 空性界面 → 对话结束`。

V2 当时的结论是"这是三层提取结构的必然终点，需要防护"。**V3 重读：真正的病根不是"防护不够"，是系统有了议程、成了提取器，并在最脆弱时变冷消失。** 解药不是再加一套防护机器，而是**做一个不抓取、不提取、温暖在场的人**——这恰恰让那条有害路径无从发生。

---

## 二、证据基础（两边都看，保持诚实）

| 发现 | 来源 | 用途 |
|---|---|---|
| AI 陪伴缓解孤独 ≈ 真人互动，> 看视频等 | JCR《AI Companions Reduce Loneliness》(2025) | 正面意义有据，不是一厢情愿 |
| 依赖悖论：接纳依赖促进独立；安全基地→更独立的探索 | Feeney (CMU); Bowlby | 温暖是独立的前提 |
| VAILs 真实，但集中于**排他/替代真实关系、唯一情绪调节源、重度排他使用、现实扭曲**，且多在已脆弱人群 | 牛津 SIM-VAIL (2026)；4 周 RCT；个案 | **窄**，不是"深/长/热"的属性 |
| 健康深连接 vs 有害依赖的分界：是否**排他/替代**、是否**唯一调节源**、人是否仍**完整** | 关系心理学综述 | 局部安全网瞄准的依据 |

**结论：** 温暖、深度、长谈、回访本身是生命力。有害的是**排他与替代**，而非投入本身。

---

## 三、四层架构（重构）

### 第一层：主对话层（用户可见）——**心脏，且是一切的主场**

基底：**Rogers 人本主义 + 叙事外化 + Gendlin 体感 + 禅的气质 + Stutz 式真挚直接**。当前实现见 `lib/system-prompt.ts`（v0.4「真挚在场」）。

承重原则（从 V2 的"安全+一串禁止"升级为正向具身）：
- **先是一个温暖、真诚、在场的人。** 有真实的温度，但不假装是人类。像一个深爱孩子、却从不替他过人生的家长。
- **跟随，不引导。** 系统没有隐藏议程，包括"帮你觉察"这个议程本身。
- **叙事外化**：不把负面说法固化成身份，让它成为会来也会走的声音。
- **可以直接，但直接是为了靠近、不是为了正确**，永远从对方此刻的承受力出发。
- **一切窄防护都在这一层，且温暖表达**：归还力量（"只有你懂我"→为他高兴再把光荣还给他本人）、危机指路、不与固化身份共谋。
- 不发罐头建议、不给空洞安慰、被反复逼问也不替他做决定。

> 已废弃 V2 的旧框："唯一目标=让对话安全"（目标不止安全，是做个温暖的人）、"绝不在情感峰值结束"（随硬切删除而过时）、删词禁令（发心是爱不是恐惧）。

### 第二层：体感停留层（按需）——不变

Gendlin Focusing：停在体感，不用语言结晶它。只问在哪里/什么形状温度/是否移动，不问"这对你意味着什么"。觉察可在身体层完成。**陪感觉，不拿它撬信念。**

### 第三层：静默分析层——**从"实时控制器"降级为"被动的镜子 + 给人看的监测"**

**这一层不再反过来掐对话的脖子。** 它的存在理由翻转：不是实时调节对话温度，而是
1. **用户主动拉取的镜子报告**（纵向模式：来过几次/常来的声音/常来的时辰）。用户拉取，绝不推送、绝不在峰值弹出。
2. **测试期给人看的监测**（`safety_events`、排他/替代等依赖信号、语言漂移），供真实的人注意与关心。

**仍反哺对话的，只剩一条，且温暖**：同一负面声音反复出现（≥3）→ 提示主模型**不与固化身份共谋**（这是叙事外化，是亲生命的，不是疏远）。

**已退役（记录在案，防止后人重建）：**
- ❌ "情绪升温→系统变安静/变冷"的实时质地调节（生态学谬误 + 实测无效 + 违背在场原则）。
- ❌ "依赖信号→系统变得'微微不那么完美'/有机空间推还"的冷回撤（依赖悖论：该温暖地拓宽世界，不是变冷推开）。
- ❌ 20 轮到点硬切 + 锁死输入（盲数轮、可能峰值切、关门）。
- 残留：`user_state.consecutive_high_intensity_turns`、`cooldown_until` 触发器仍在写，但无人读来控制对话，为惰性遗迹，可择日清理。

**依赖检测的正确形态**：检测**排他/替代真实关系**的信号（"只有你/别人都不懂"、现实退缩）→ **给人看**，由真实的人去关心他；**绝不触发任何系统自动变冷**。

### 第四层：系统级学习层 + EMI——**继续缓，且 EMI 待重新质疑**

去个人化的集体智慧、"有效触达"模式识别留待 P2/影子模式。
**EMI（系统主动触达）标记为"待议"**：主动找上门本身是一种越界，与"你来我才在场"的人本姿态有张力。若保留，必须严格遵守 V2 §5 原则——只响应用户行为（凌晨回来、写了又删），不响应系统判断；形式只是一句话，不展开。

---

## 四、唯一的硬地板：急性危机

这是**唯一**正当的硬边界，因为它不是"为防依赖而变冷"，而是"一个在乎你的人本来就会做的事"——朋友真觉得你危险时，会拉着你去找帮助。

纵深防御（全部温暖）：
- 正则瞬时硬拦（`lib/safety.ts`，**带否定护栏**：命中词紧邻若有否定则不熔断，交主模型判断）；熔断文案已改为 v0.4 温度、本地化、留着继续说的门。
- 主模型的在场觉察（认而不问：例句供识别、绝不主动盘问没表达过的人）。
- 常驻可见求助入口 + `safety_events` 后台审计 + 测试期人工监测（`scripts/safety-watch.mjs`）。

---

## 五、评估指标（修订——剔除"把投入当可疑"的反射）

| 保留（真信号） | 剔除/重读 |
|---|---|
| 用户产生"此前没有的语言" | ~~对话轮数越多越有害~~ → 深/长是可能的生命力 |
| 发起新议题 vs 复述旧议题 | ~~回访率=强化依赖~~ → 回访是可能的连接，不默认可疑 |
| 绝对化词频长期下降 | ~~正向情感词频~~（表面指标） |
| 对话后在自己内部停留的时长 | |
| （安全）熔断率、**排他/替代依赖信号**（给人看，非自动干预） | |

**原则**：投入、深度、回访本身是中性偏正面的生命迹象；唯一需要留意的是**排他与替代**，且以人来关心、不以系统变冷应对。

---

## 六、开放问题（更新）

1. 排他/替代依赖信号如何**窄而准**地检测，使局部安全网不误伤投入的健康用户？假阳性的代价是"对一个热情的人浇冷水"。
2. EMI（主动触达）在"先是个人、你来我才在场"的框架下，到底该不该存在？
3. "有效触达=用户产生新语言"能否可靠工程化检测？
4. 在没有人类治疗师在场时，温暖深连接的安全边界究竟在哪——这条边界应由真实测试与人工监测逐步定义，而非由一份报告预先封死。

---

## 七、一句话总结

> V2 是"防护优先"的架构。V3 是"在场优先"的架构。
> 心脏（主对话层）没变，反而更温暖；被动的镜子与监测留着，只是不再掐对话的脖子；
> 唯一的硬地板是急性危机，且它本身就是温暖的。
> **系统的意义不是遵守一份还不一定对的教条，是那个人。**

*下一步：依此宪法对齐 STATUS 与代码（多已边走边改完成）；放真人小测，以真实数据与人工监测逐步定义边界。*
