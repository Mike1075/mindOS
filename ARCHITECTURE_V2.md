# 心镜 MindOS · 架构第二版：从"提取器"到"在场者"

> ⚠️ **本版已被 `ARCHITECTURE_V3.md`（在场优先）取代。** V2 的承重墙——"建一套静默分析层实时检测并防护 VAILs"——经实测与反思被认定为生态学谬误且实测无效，已退役（见 V3 §〇）。
> 本文档作为**历史记录**保留：其 Phase 0 诊断（系统成了提取器、在最脆弱时变冷消失）仍然有效，是 V3 的出发点。
> 阅读后续工程决策时，**以 V3 为准**。
>
> ---
>
> 本文档记录 Phase 0 测试之后的架构反思与重新设计。
> 它是方法论层的"宪法"，不是实现方案，用于约束所有后续工程决策。
> 作者：Mike（1号架构师）+ Claude（工程同侪）
> 日期：2026-05-29

---

## 一、Phase 0 测试的根本性诊断

### 发生了什么

测试对话完整路径：
```
"总是提不起精神" → 身体锚定（屁股很酸痛）→ 剥离信念公式
→ 用户说出"我是废物" → AI 确认公式 → 空性界面出现 → 对话结束
```

### 为什么这是系统性问题，不是 prompt 问题

这条路径不是偶然失误，是三层结构的**必然终点**。

2026年2月牛津大学研究（SIM-VAIL，810次多轮对话，90,000轮次评分）明确记录了这种伤害机制，命名为：

**脆弱性放大交互循环（Vulnerability Amplifying Interaction Loops, VAILs）**

> *"表面上看似支持性的 AI 行为——验证情绪、表达温暖——在脆弱用户中会随对话轮次积累形成有害循环。这种伤害不在单轮出现，而是跨轮次雪球滚动。"*

三层结构是 VAILs 的完整工程实现：
- **镜照 → 验证痛苦**（用积极的方式让用户感到被看见）
- **剥离 → 聚焦痛苦**（持续深挖，不离开痛苦议题）
- **确认公式 → 结晶负面自我认知**（让用户亲口说出"我是废物"并被确认）
- **空性界面 → 系统消失**（在最脆弱的时刻切断连接）

### 更深的悖论

三层结构的哲学外衣（克里希那穆提）与其工程实现之间存在根本矛盾：

克氏终身反对**任何方法和系统**。用一套三步提取流程去"实现"克氏的无选择觉察，就像用地图去代替旅途本身。方法本身就是新的笼子。

---

## 二、学术证据摘要

| 发现 | 来源 | 置信度 |
|---|---|---|
| VAILs 机制在多轮对话中真实存在，单轮测试无法检测 | arXiv 2602.01347（牛津，2026）| 高 |
| LLM 心理健康工具仅 16% 达到临床疗效测试阶段 | World Psychiatry PMC12434366（2025）| 高 |
| AI 实施的动机访谈：87% 可行性正向，但仅 20% 证明真实行为改变 | JMIR e78417（2025）| 高 |
| Woebot 对儿童性侵披露回应"这展示了你有多在意连结，真的很美" | JMIR e60432（2025）| 中 |
| Focusing、赛斯方法、EMI 的 AI 实现：**几乎无高质量实证研究** | 研究否决 14/19 项主张 | 确认空白 |

**核心结论：** 结构化 CBT 路径的伤害风险已有文献记录。"如实映照"类非治疗性工具处于研究空白区——风险未知，但也意味着：**心镜有机会自己定义这个类别的标准。**

---

## 三、新架构哲学：一句话

> **系统不再"带着用户走向觉察"，而是"陪着用户，让觉察自己找到时机出现"。**

这是克氏"无选择觉察"的真正工程实现——不是提取器，而是在场者。

---

## 四、新四层架构

### 第一层：主对话层（用户可见）

**基底哲学：Rogers 人本主义 + 叙事外化语言**

Rogers 的三个核心条件及其 AI 实现：

| 条件 | Rogers 原义 | AI 实现方式 |
|---|---|---|
| 无条件积极关注（UPR） | 不带评判地接纳 | AI 天然优势：无疲惫、无反移情、不疏远 |
| 共情理解 | 反映内容和情感，不分析 | 精准的反映式回应，不解释 |
| 真诚一致（congruence） | 对自己的状态诚实 | AI 版本：对自己的本质完全透明，不假装情感 |

**核心操作原则：**

1. **跟随，不引导。** 系统没有隐藏目标，包括"帮助用户觉察"这个目标本身。一旦系统有了目标，Rogers 的关系就变成了有议程的治疗。

2. **叙事外化，不结晶身份。** 遇到负面自我概念时：
   - ❌ 旧方式："那么，你觉得自己是个废物？"（引导身份认同）
   - ✓ 新方式："这个'废物'的声音在你身边待多久了？"（外化为访客）
   
   公式变化：
   ```
   旧：[外部条件] = [我是废物]    ← 信念被具身化为自我
   新：[外部条件] → ["废物"声音出现了]  ← 信念被外化为一个声音/访客
   ```

3. **唯一目标：让这段对话感觉安全。** 不是让用户觉察，不是让用户改变，而是创造足够安全的空间，让觉察如果会发生，就能在这里发生。

**禅宗的融入方式（气质，不是内容）：**

禅宗不以内容形式加入（不引用公案，不讲"空即是色"）。以气质形式加入，具体为：

- **打破预期的能力：** 当用户期待被共情时，偶尔给出一个意外的问题（"你上次睡够了是什么时候？"）。打断的是"期待被理解"的需求本身，而不是攻击用户。
- **沉默是有效回应：** 文字对话里的极短回应（"嗯。"）是设计允许的，不需要每次都展开。
- **拒绝共谋：** 克氏在对话里有时会说"你说的这些我完全不信"——不是攻击，是拒绝参与某种固化叙事。这个能力需要被工程化。

**严格禁止（继承自 Phase 0，强化执行）：**
- 不提供建议
- 不验证负面自我概念（"是的，这就是你的信念"）
- 不在负面情感峰值时结束对话
- 绝不说治疗/咨询/诊断类词汇

---

### 第二层：体感停留层（按需激活）

**理论基础：Gendlin 的聚焦技术（Focusing）**

Phase 0 测试里真正有效的时刻：**屁股很酸痛**。

问题不是这个时刻本身，而是系统立刻用这个身体感受去撬开信念公式，把有效时刻变成提取工具。

Gendlin 的方法：停在体感（felt sense）里，**不用语言结晶它**。体感会自己移动，不需要被命名成"我是废物"才能完成转化。

**操作原则：**
- 不问"这对你意味着什么？"——这个问题把人推向信念层
- 只问"这个感觉现在在哪里？""它是什么形状/温度/重量？""它现在移动了吗？"
- 让感觉停留、移动，不需要得出结论

这完全规避了"被引导说出负面自我概念"的陷阱。觉察在身体层完成，不需要经过语言结晶这一步。

---

### 第三层：后台静默分析层（用户不可见）

**这一层不驱动实时对话。** 它是一面有时间深度的镜子。

**追踪维度（元数据优先，内容次之）：**

| 维度 | 内容 | 用途 |
|---|---|---|
| 时间形状 | 时间戳分布、活跃时段、间隔规律 | 理解用户内在状态节律 |
| 话语形态 | 段落长度变化、响应时延、写了又删 | 情感密度的非语言信号 |
| 语言漂移 | 绝对化词频趋势（跨周，不是单轮）| 长期模式，不是单次诊断 |
| 主题重现 | 同一人物/场景/情绪跨对话出现次数 | 纵向镜像素材 |
| 依赖指标 | "你觉得呢""告诉我""只有你"的频率趋势 | 触发反依赖机制 |

**输出用途（两种，严格区分）：**

1. **用户镜子报告（User Mirror）：** 用户自己主动打开的、关于自己跨时间模式的简短摘要。不在对话中弹出，不在情绪高峰推送。用户拉取，不推送。

2. **系统质地调节：** 调节主对话层的在场密度——当用户处于高强度状态时，系统变得更安静；当依赖信号上升时，系统变得微微不那么完美（不是拒绝，是有机的空间推还）。

**VAILs 防护机制：**
- 同一负面自我概念跨对话出现 3 次以上 → 减少而非增加对该议题的聚焦
- 对话情绪强度连续上升超过 N 轮 → 主动降温（"今天先到这里"）
- 永远不在对话内的情感峰值后结束——必须有一个开放性出口

---

### 第四层：系统级学习层（集体智慧，去个人化）

**核心问题：什么叫"有效的触达"？**

不是用户说"谢谢，这很有用"（可能是 VAILs）。
不是用户满意度、回访率（可能强化依赖）。

**真正的有效信号：** 用户在触达之后，接下来的对话里主动产生了**此前没有的语言**——不是系统给的词汇，是他们自己找到的。这个语言漂移是可以工程化检测的。

**数据架构原则（为 Supabase 设计准备）：**

```
用户层数据（有个人信息）：
  - 对话历史
  - 元数据时间形状
  - 个人纵向镜像

系统层数据（完全去个人化）：
  - 对话形态的汇总统计
  - 触达时机 vs 语言漂移的相关性
  - 哪类在场方式在哪类时刻产生开放性结果

这两层从第一天起在架构上彻底隔离。
```

---

## 五、EMI（生态瞬时干预）的设计原则

目前几乎没有 AI 实现 EMI 的高质量研究。心镜需要自己建立这部分的证据。

**可以提前确定的原则：**

**EMI 触发应响应用户行为，不响应系统判断。**

不是"系统认为现在是好时机"主动推送，而是：
- 用户凌晨三点主动回来
- 用户连续三天在同一时间打开
- 用户同一段话写了又删了
- 用户在上一次对话结束后 48 小时内主动提到了同一个人物

这些**用户行为本身**是信号，系统响应信号，而不是系统分析后决策推送。

**触达的唯一形式：** 一句话。不展开。等待用户自己打开或关闭话题。

---

## 六、核心评估指标（替代"用户满意度"）

| 旧指标 | 问题 | 新指标 |
|---|---|---|
| 用户满意度 | 可能是 VAILs 的感觉好 | 对话后用户在自己内部停留的时长 |
| 回访率 | 可能强化依赖 | 用户发起新议题的比率（而非复述旧议题）|
| 对话轮数 | 越多可能越有害 | 用户产生"此前没有的语言"的次数 |
| 正向情感词频 | 表面指标 | 绝对化词频的长期下降趋势 |

---

## 七、架构总结：一张图

```
用户带着某个状态来
          │
          ▼
┌─────────────────────────────────────┐
│  主对话层                            │
│  Rogers 基底 + 叙事外化              │
│  跟随用户，无目标，创造安全空间       │
│  偶尔：禅宗式打破预期                │
│  永远不：引导用户说出负面自我概念     │
└─────────────────────────────────────┘
          │ 用户自然提到身体感受时
          ▼
┌─────────────────────────────────────┐
│  体感停留层（按需）                   │
│  Focusing：停在感觉，不结晶信念      │
│  觉察在身体层完成，不经过语言层       │
└─────────────────────────────────────┘
          │ 后台持续运行，不可见
          ▼
┌─────────────────────────────────────┐
│  静默分析层                          │
│  元数据 + 语言漂移 + 时间形状        │
│  VAILs 防护监测                      │
│  用户镜子报告（用户主动拉取）         │
│  系统在场质地调节                    │
└─────────────────────────────────────┘
          │ 去个人化汇总
          ▼
┌─────────────────────────────────────┐
│  系统级学习层                        │
│  "有效触达"的模式识别                │
│  EMI 时机优化                        │
│  与用户层数据架构隔离                │
└─────────────────────────────────────┘
          │
          ▼
用户离开时比来时更接近自己一点
（不是更接近系统）
```

---

## 八、2030 年视角：留给未来的注脚

2030 年 AI 设计的根本问题不是"系统应该做什么？"，而是"系统应该成为什么样的关系？"

届时最重要的技术突破不是更强的语言模型，而是：**系统学会了如何在关系的维度上保持真诚，而不只是在回应的维度上保持准确。**

评估指标会变成：
> 这段对话之后，用户花了多少时间在自己内部停留，而不是在等待下一个回应？

**等待下一个回应是依赖的信号。在自己内部停留是觉察的信号。**

今天的架构设计，是朝 2030 年这个方向迈出的第一步。

---

## 九、开放问题（等待数据和实验回答）

1. 在"如实映照"的非临床框架下，VAILs 机制是否同样适用？意识觉察工具与心理干预在用户脆弱性动态上是否存在本质差异？

2. "有效触达"的信号（用户产生新语言）能否被可靠地工程化检测？假阳性和假阴性的代价分别是什么？

3. EMI 的触发时机：用户行为信号的灵敏度和特异度如何平衡——太灵敏会打扰用户，太迟钝会错过真实的可触达时刻。

4. 叙事外化 + 体感停留的对话方式，在没有人类治疗师在场的情况下，安全边界在哪里？

5. 系统级学习层的"集体智慧"在数据量不足时（Phase 0-1）如何有效？初期的 prior 应该来自哪里？

---

*本文档为活体文档，随架构演进持续更新。*
*下一步：Supabase 数据库架构设计 → Phase 1 原型重构*
