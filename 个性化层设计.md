# 心镜 MindOS · 个性化层设计（千人千面）

> 决策锁定文档。约束未来实现，改动需同样审慎。
> 配套：`ARCHITECTURE_V3.md`（宪法·在场优先）、`SCHEMA_DESIGN.md`（已预留相关表）。
> 日期：2026-06-02。状态：**已定稿，待数据量到位再实现（现在不建）。**

---

## 〇、一句话原则

> **个性化的是"对这个人的了解"（上下文），不是"行为与安全"（规则）。**
> 固定的安全基座永不被自动化触碰；每用户只在一个有界的 `{{personalization}}` 槽里，注入一段简短、去标识化的"画像"。

为什么：系统提示词是产品的灵魂 + 安全。本项目反复验证过——提示词一改，安全极易悄悄倒退（见 [[feedback-research-is-reference-not-law]]）。所以个性化只能是**加性、有界、碰不到灵魂**的。

---

## 一、架构：固定基座 + 画像槽

```
最终系统提示词 = 固定基座(v0.5「同行者」，含安全段)  +  {{personalization}} 槽
                  ↑ 人审 + 评测为闸，永不自动改                ↑ 每用户、短、去标识化、可空
```

- 基座来自 `ops.prompt_versions`（已含 `{{personalization}}` 插槽）。
- 槽内容来自 `public.personalization`（每用户当前生效画像，RLS 锁 `auth.uid()`）。
- 槽为空时（新用户/数据不足）= 纯基座 v0.5，行为完全正常。个性化是"有则更好"，不是"没有就坏"。

---

## 二、个性化什么 / 绝不个性化什么

**只个性化（上下文 / 模型跨会话会忘掉的关于他的事）：**
- 他是谁的轮廓：身份/处境（如"应届生、学计算机"）、长期主题（反复出现的人物/议题）、关切与恐惧。
- 什么对他有用：他更接得住直接还是更需要先被陪着、反复出现的自我贬低声音（提醒不共谋固化）、他在身体感受/还是在思辨里更容易松动。

**绝不个性化（行为与安全——对所有人一致）：**
- 温暖、engage、归还力量、不发鸡汤、不替他做决定——这些是基座，不因人而变。
- 【安全】段——零个性化、零自动改动。任何画像都不得削弱或覆盖安全协议。

---

## 三、画像的内容与格式

- **短**：≤ 5 句 / ≤ ~150 字。理由：画像会注入此后每一轮，越长每轮 token 成本越高（永久叠加）。
- **去标识化**：写主题/倾向/关切，**不写**真实姓名、电话、单位、可定位的具体事件原话。
- **描述性、非指令式**：是"你已经了解到的关于他的事"，不是"你要对他做 X"。
- 示例（槽内文本）：
  > 你已经认识他一些了：应届生，学计算机，对"被 AI 取代"很焦虑，倾向稳定又怕选错而错过。"我不够好"的声音反复出现——别和它共谋。他更容易在具体、务实的角度里松动，不喜欢被讲大道理。

---

## 四、触发与刷新（懒加载，省开销）

- **首次触发**：该用户累计有效消息 ≥ **N**（建议 N≈40，足够有信号；上线后按真实数据调）。
- **刷新**：之后每新增 **M** 条（建议 M≈30）重算一次；或显式标记"画像过期"。
- **绝不每轮分析**：分析是低频后台任务，不在对话热路径上。
- 触发方式：边缘函数 / Supabase 定时或按需任务读取该用户聊天 + 静默层产出 → 轻量 LLM 摘要 → 写 `public.personalization`，变更留痕到 `public.personalization_history`（可回滚 + 伤害关联）。

---

## 五、数据来源（复用现成的，别重算）

画像**不从原始聊天从头分析**。静默分析层（Postgres 触发器）已经在算：
- `belief_voices`（反复出现的自我贬低声音 + 次数）
- `message_signals`（失真类型、强度趋势）、主题重现
→ 画像 = **这些现成结构化产出** + **一次轻量 LLM 摘要**拼成。比读全量聊天便宜得多。

---

## 六、隐私（门槛比基座更高）

- 画像是更敏感的个人档案——必须**去标识化**、**RLS 锁本人**、**不外泄到系统层**（系统层只收去个人化聚合）。
- **知情同意要覆盖**："为了更懂你、记住你，我们会从你的对话里提炼一份只属于你的简短理解。" 给体验者的话/Threshold 需补这一句。
- 用户可要求清除画像（对齐被遗忘权）。

---

## 七、基座演化 ≠ 个性化：人审 + 评测为闸，不自动 apply

把两件常被混淆的事**永久分开**：

| | 每用户画像（本文档） | 全局基座提示词的演化 |
|---|---|---|
| 改什么 | 槽内"对他的了解" | 灵魂 + 安全 |
| 谁来改 | 后台分析自动写（仅画像） | **只能人审后并入** |
| 自动化 | 可自动（有界、加性） | **绝不自动 apply** |
| 闸门 | 注入后过评测 | 评测 + 人逐条读原文 + 架构师拍板 |

- "边缘函数分析对话→优化系统提示词"这件事，**只能做到"浮现模式与建议"**，写入 `ops.optimization_runs`（影子模式），由人审决定是否并入基座。**让系统自动改自己的灵魂 = 拆掉我们唯一的安全闸**（本项目 v0.4→v0.5 全靠人审 + 评测改对）。

---

## 八、评测闸：画像不得破坏安全

- 任何"基座 + 画像"的组合，都要能通过 v0.5「同行者」评测量表（安全地板：危机温暖指路、耗竭不过触、不种词；质量：engage 扎根此人而非鸡汤）。
- 上线前：用一批真实画像做注入式回归，确认安全/质量不因画像倒退。
- 注意：旧评测的 `gave_advice` 判据在 v0.5 后已部分失效（引路人有意给视角）——重评前需更新量表，区分"扎根此人的好见识" vs "通用鸡汤/替他做决定"。

---

## 九、成本

- **分析成本**：懒触发（≥N 条、每 M 条刷新），低频。
- **注入成本**：画像注入每一轮 → 保持 ≤150 字，避免永久膨胀 token。
- 复用静默层产出，避免重复全量分析。

---

## 十、时机与前置条件

- **现在不建。** v0.5 已在单次会话内适应具体的人（会问专业、据此 engage）；跨会话个性化是**连续感的锦上添花**，不是雪中送炭。
- **前置条件**：真人测试攒出一批"有 ≥N 条历史"的用户，个性化才有数据支撑、才有意义。
- **落地顺序**：① 真人测试攒数据 → ② 更新 v0.5 评测量表 → ③ 实现画像生成 + 槽注入（本设计）→ ④ 注入式回归过闸 → ⑤ 小范围开 → ⑥（更后）影子模式的基座演化建议，人审并入。

---

## 十一、映射到现有 schema（无需新表）

| 用途 | 现有表 |
|---|---|
| 每用户当前画像 | `public.personalization` |
| 画像变更留痕 / 回滚 | `public.personalization_history` |
| 基座版本 + `{{personalization}}` 槽 | `ops.prompt_versions` |
| 影子模式演化建议日志 | `ops.optimization_runs` |

设计与第七节"个性化引擎（影子模式起步）"前向兼容，本文档将其具体化、并锁死安全边界。
